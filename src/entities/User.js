class User {
    constructor(Obj){
        this.id = Obj.id
        this.name = Obj.name
        this.aboutShort = Obj.aboutShort
        this.lastPostDate = Obj.lastPostDate
        this.avatarUrl = Obj.avatarUrl
    }
}

export default User
